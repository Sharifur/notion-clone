"use client";

interface NavbarProps{
    isCollapsed: boolean;
    onResizeWith: () => void;
}

export const Navbar = ( {isCollapsed, onResizeWith} : NavbarProps) => {

    return (
        <>
            Navbar !
        </>
    )
}

